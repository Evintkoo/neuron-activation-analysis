# Assets

Place `brain.obj.gz` here to use a real brain mesh.

If absent, the viewer falls back to an icosahedron approximation.

## Obtaining a brain mesh

Any Wavefront OBJ file of a brain surface (e.g. from FreeSurfer or a public atlas)
can be gzip-compressed and dropped here as `brain.obj.gz`.
